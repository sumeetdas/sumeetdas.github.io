{
  "scripts": {
    "css": "tailwindcss -i ./style.css -o ./public/output.css",
    "fsbuild": "dotnet build",
    "build": "npm run fsbuild && dotnet run"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.7"
  }
}
